---
local:
  hosts:
    localhost:
all:
  vars:
    domain: # your domain to build fqdn
    proxy: ### Set if you need proxy for yum.conf

mapr:
  hosts:
    192.168.0.[101:103]: ### UPDATED FOR YOUR NET
  vars:
    ansible_user: root ## user name to connect to template
    mapr_user: mapr
    mapr_group: mapr
    mapr_uid: 5000
    mapr_gid: 5000
    mapr_pass: mapr123 ### DO NOT EDIT
    ssh_private_key_file: ### user private key to use in VMs
